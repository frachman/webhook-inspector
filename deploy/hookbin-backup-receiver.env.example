# Copy to /etc/hookbin/backup-receiver.env on the homelab and chmod 0600.
HOOKBIN_RECEIVER_INCOMING=/data/backups/hookbin/incoming
HOOKBIN_RECEIVER_VERIFIED=/data/backups/hookbin/verified
HOOKBIN_RECEIVER_RETENTION_DAYS=30
